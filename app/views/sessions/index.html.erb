<h2><%= link_to "Home", root_path %></h2>
<h2><%= link_to "Add a Session", new_session_path %></h2>

<table class="table">
  <thead>
    <th> Session </th>
  </thead>
  <% @sessions.each do |session| %>
    <tr>
      <td class="col-md-2"><%= link_to session.session, session_path(session) %></td></td>
    </tr>
  <% end %>
</table>
