== README

This repo represents the completion of another timed code test.  It was one of those
instances when you walk away, come back to the task, and see a glaring oversight
and finish everything up in 10 minutes.

To run locally:
```bundle```
```rake db:create```
```rake db:migrate```
```rake db:seed```
```rails s```
